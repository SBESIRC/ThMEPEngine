<UserControl x:Class="TianHua.Electrical.PDS.UI.UserContorls.ThPDSDistributionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="clr-namespace:HandyControl.Controls;assembly=HandyControl"
             xmlns:cvt="clr-namespace:TianHua.PDS.UI.Converter"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="1000" d:DesignWidth="1000">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <cvt:RadioUiValueConverter x:Key="RadioUiValueConverter" ></cvt:RadioUiValueConverter>
            <cvt:VisibilityHiddenConverter x:Key="VisibilityConverter"></cvt:VisibilityHiddenConverter>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom"  FontSize="14">
            <TextBlock>进度</TextBlock>
            <Separator Width="1"  HorizontalAlignment="Center"/>
            <ProgressBar Width="100" Height="10" Value="50"></ProgressBar>
            <Separator Width="1"  HorizontalAlignment="Center"/>
            <TextBlock>正在计算回路电流:1321/2164</TextBlock>
            <Separator Width="1"  HorizontalAlignment="Center"/>
            <TextBlock>报错信息</TextBlock>
            <Separator Width="1"  HorizontalAlignment="Center"/>
            <TextBlock>0个错误信息</TextBlock>
            <Separator Width="1"  HorizontalAlignment="Center"/>
            <TextBlock>更新状态</TextBlock>
            <Separator Width="1"  HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal">
                <Rectangle Fill="Yellow" Width="14" Height="14"></Rectangle>
                <TextBlock>未更新</TextBlock>
            </StackPanel>
            <Separator Width="1"  HorizontalAlignment="Center"/>
            <TextBlock>登录状态</TextBlock>
            <Separator Width="1"  HorizontalAlignment="Center"/>
            <TextBlock ><Hyperlink>已登录</Hyperlink>@thape.com.cn</TextBlock>
        </StatusBar>
        <Grid Width="950" Height="700">
            <Border BorderBrush="Black" BorderThickness="1"  Background="White" Margin="1,0,267,117">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Canvas Background="Transparent" x:Name="canvas" Style="{x:Null}" Width="10000" Height="10000"></Canvas>
                </ScrollViewer>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Background="White" Margin="688,0,10,442">
                <TreeView x:Name="tv" ItemsSource="{Binding DataList}">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding DataList}">
                            <TextBlock Text="{Binding Name}"/>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                    <TreeView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="消防电源监控" IsCheckable="True" IsChecked="{Binding Path=Current.FirePowerMonitoring}"/>
                            <MenuItem Header="电气火灾监控"  IsCheckable="True" IsChecked="{Binding Path=Current.ElectricalFireMonitoring}"/>
                            <MenuItem Header="浪涌保护器">
                                <MenuItem Header="无" IsCheckable="True" IsChecked="{Binding Path=Current.SurgeProtection, Converter={StaticResource RadioUiValueConverter}, ConverterParameter=None}"/>
                                <MenuItem Header="SPD1" IsCheckable="True" IsChecked="{Binding Path=Current.SurgeProtection, Converter={StaticResource RadioUiValueConverter}, ConverterParameter=SPD1}"/>
                                <MenuItem Header="SPD2" IsCheckable="True" IsChecked="{Binding Path=Current.SurgeProtection, Converter={StaticResource RadioUiValueConverter}, ConverterParameter=SPD2}"/>
                                <MenuItem Header="SPD3" IsCheckable="True" IsChecked="{Binding Path=Current.SurgeProtection, Converter={StaticResource RadioUiValueConverter}, ConverterParameter=SPD3}"/>
                                <MenuItem Header="SPD4" IsCheckable="True" IsChecked="{Binding Path=Current.SurgeProtection, Converter={StaticResource RadioUiValueConverter}, ConverterParameter=SPD4}"/>
                            </MenuItem>
                        </ContextMenu>
                    </TreeView.ContextMenu>
                </TreeView>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Background="White" Margin="688,263,10,117">
                <hc:PropertyGrid x:Name="propertyGrid" />
            </Border>
            <TextBox x:Name="tbxConsole" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Visible" IsReadOnly="True" Style="{x:Null}" Margin="6,588,225,65"></TextBox>
            <Button Click="btnBalance" Style="{x:Null}" Margin="725,588,121,92">平衡相序</Button>
            <Button Click="btnSelectFast" Style="{x:Null}" Margin="834,588,13,92">快速选型</Button>
            <Button Click="btnGenMulti" Style="{x:Null}" Margin="725,611,119,69">批量生成</Button>
            <Button x:Name="btn" Click="btnGenSingle" Style="{x:Null}" Margin="834,612,10,68">单独生成</Button>
        </Grid>
    </DockPanel>
</UserControl>
