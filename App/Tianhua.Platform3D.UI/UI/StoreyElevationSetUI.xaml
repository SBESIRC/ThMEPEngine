<UserControl x:Class="Tianhua.Platform3D.UI.UI.StoreyElevationSetUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tianhua.Platform3D.UI.UI"
             mc:Ignorable="d" >
    <Grid Margin="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Style="{x:Null}" Panel.ZIndex="3"  HorizontalAlignment="Right" Margin="0,0,5,0" Name="btnAddBuilding" Content="新增单体" Width="80" Height="20" Click="btnAddBuilding_Click"></Button>
        <TabControl Style="{x:Null}" Panel.ZIndex="0" Margin="0,-25,0,0" Grid.Row="1" Name="tabStorey" TabStripPlacement="Top" MouseDoubleClick="tabControl1_MouseDoubleClick" 
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch"
                    >
            <TabControl.ContextMenu>
                <ContextMenu>
                    <MenuItem Name="copyItem" Header="复制" Click="copyItem_Click"/>
                    <MenuItem Name="pasteItem" Header="粘贴" Click="pasteItem_Click"/>
                    <MenuItem Name="deleteItem" Header="删除" Click="deleteItem_Click"/>
                    <MenuItem Name="modifyItem" Header="修改" Click="modifyItem_Click"/>
                    <MenuItem Name="editStoreyItem" Header="编辑楼层" Click="editStoreyItem_Click"/>
                </ContextMenu>
            </TabControl.ContextMenu>                  
        </TabControl>
    </Grid>
</UserControl>
